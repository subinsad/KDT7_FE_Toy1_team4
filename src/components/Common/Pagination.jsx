import React from "react";
import "./Pagination.scss";

const Pagination = () => {
  return (
    <>
      <div className="pagination">
        <button>이전</button>
        <button>다음</button>
      </div>
    </>
  );
};

export default Pagination;
